{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to dotnet",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}